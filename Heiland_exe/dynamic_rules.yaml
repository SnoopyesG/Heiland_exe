audio:
  fetzen_dir: "./session/fetzen"
  parallel: 3
  delay: 3
  threshold_db: -50
  min_silence: 0.30
router:
  recency_half_life_h: 12 # neuere Fetzen werden stärker gewichtet
  cooldown_s: 60 # selbe „Basis“ nicht zu schnell wieder
  bias_keywords: [] # wird live überschrieben (bias.txt)
  bias_weight: 2.0
  random_jitter: 0.35 # Zufallseinfluss 0..1
  max_queue: 200
